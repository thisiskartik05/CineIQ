# CineIQ backend – production dependencies only
# Generated from the full environment; stripped of Jupyter, CUDA, Streamlit, etc.
# Install with:  pip install -r requirements.txt

# ── Web server ────────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.48.0
starlette==1.2.1
python-multipart==0.0.30

# ── Data / ML ─────────────────────────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0          # TF-IDF vectorizer + cosine_similarity
scipy==1.17.1                 # sparse matrix operations (used by sklearn)
torch==2.12.0                 # LightGCN embeddings inference
torch-geometric==2.7.0        # LGConv layer (needed to load trained model)
vaderSentiment==3.3.2         # sentiment scoring in preprocess_tfidf.py

# ── Database ──────────────────────────────────────────────────────────────────
pymongo==4.17.0               # MongoDB Atlas driver
dnspython==2.8.0              # required by pymongo SRV URI parsing

# ── HTTP / utils ──────────────────────────────────────────────────────────────
requests==2.34.2              # TMDB poster fetching in migrate.py
pydantic==2.13.4              # FastAPI request/response models
certifi==2026.2.25            # SSL cert bundle
requests==2.31.0